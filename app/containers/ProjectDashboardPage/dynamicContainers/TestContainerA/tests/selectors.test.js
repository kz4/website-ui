// import { fromJS } from 'immutable';
// import { makeSelectTestContainerADomain } from '../selectors';

// const selector = makeSelectTestContainerADomain();

describe('makeSelectTestContainerADomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
