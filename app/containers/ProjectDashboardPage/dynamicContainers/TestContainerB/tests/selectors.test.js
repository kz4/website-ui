// import { fromJS } from 'immutable';
// import { makeSelectTestContainerBDomain } from '../selectors';

// const selector = makeSelectTestContainerBDomain();

describe('makeSelectTestContainerBDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
