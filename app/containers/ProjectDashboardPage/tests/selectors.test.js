// import { fromJS } from 'immutable';
// import { makeSelectProjectDashboardPageDomain } from '../selectors';

// const selector = makeSelectProjectDashboardPageDomain();

describe('makeSelectProjectDashboardPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
