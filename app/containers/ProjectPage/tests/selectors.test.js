// import { fromJS } from 'immutable';
// import { makeSelectProjectPageDomain } from '../selectors';

// const selector = makeSelectProjectPageDomain();

describe('makeSelectProjectPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
