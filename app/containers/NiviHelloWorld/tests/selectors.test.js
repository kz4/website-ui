// import { fromJS } from 'immutable';
// import { makeSelectNiviHelloWorldDomain } from '../selectors';

// const selector = makeSelectNiviHelloWorldDomain();

describe('makeSelectNiviHelloWorldDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
