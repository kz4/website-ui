import React from 'react';
import { shallow } from 'enzyme';

import NavBar from '../NavBar';

// TODO
describe('<NavBar />', () => {
  it('Expect to have unit tests specified <Navbar>', () => {
    expect(true).toEqual(false); 
  });
});
