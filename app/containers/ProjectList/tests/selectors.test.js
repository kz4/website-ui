// import { fromJS } from 'immutable';
// import { makeSelectProjectListDomain } from '../selectors';

// const selector = makeSelectProjectListDomain();

describe('makeSelectProjectListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
