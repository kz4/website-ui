// import { fromJS } from 'immutable';
// import { makeSelectCreateEditProjectPageDomain } from '../selectors';

// const selector = makeSelectCreateEditProjectPageDomain();

describe('makeSelectCreateEditProjectPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
