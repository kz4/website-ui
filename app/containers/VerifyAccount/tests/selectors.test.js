// import { fromJS } from 'immutable';
// import { makeSelectVerifyAccountDomain } from '../selectors';

// const selector = makeSelectVerifyAccountDomain();

describe('makeSelectVerifyAccountDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
