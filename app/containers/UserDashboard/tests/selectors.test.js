// import { fromJS } from 'immutable';
// import { makeSelectUserConsoleDomain } from '../selectors';

// const selector = makeSelectUserConsoleDomain();

describe('makeSelectUserConsoleDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
