// import { fromJS } from 'immutable';
// import { makeSelectDogsDomain } from '../selectors';

// const selector = makeSelectDogsDomain();

describe('makeSelectDogsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
