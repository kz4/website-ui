// import { fromJS } from 'immutable';
// import { makeSelectCatsDomain } from '../selectors';

// const selector = makeSelectCatsDomain();

describe('makeSelectCatsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
